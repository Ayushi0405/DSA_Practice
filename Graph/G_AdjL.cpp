#include<iostream>
using namespace std;

int main(){
    vector<int> adj[n+1];
    int n,m;
cin>>n>>m;
for(int i=0;i<m;i++){
    int u,v;
    adj[u].push_back(v);
    adj[v].push_back(u);
}
return 0;
}